{{ template "common/header.tmpl" .}}
<div class="row">
    <div class="col-6">

    <p><a href="{{route "wallet.create"}}" class="btn btn-primary">Добавить</a></p>


    <div class="card">
    
    <div class="card-body table-responsive p-0">
        <table class="table table-hover text-nowrap">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Currency</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {{ range .wallets }}
                <tr>
                    <td><a href="{{route "wallet.update" .Id}}">{{ .Name }}</a></td>
                    <td>{{ .Currency }}</td>
                    <td><a href="#"><i class="nav-icon fas fa-solid fa-trash"></i></a></td>
                </tr>
                {{ end }}
            </tbody>
        </table>
    </div>
    
    </div>
    
    </div>
    </div>
{{ template "common/footer.tmpl" .}}
